# מה נשאר לעשות

מצב נכון ל-22.9.2026. המנוע שלם ומוכח בבדיקות; **אין עדיין אפליקציה
שקוראת לו, והוא מעולם לא רץ על מסד אמיתי.**

מה שכן עובד: 216 בדיקות עוברות, `flutter analyze` נקי, ומבחן השקילות
(`test/equivalence_test.dart`) מוכיח שסינון patch שקול לגיזום-אחרי-החלה.

---

## 1. ‏[חוסם] להריץ על מסד אמיתי

**שום דבר כאן מעולם לא נגע ב-7.4GB.** כל 216 הבדיקות רצות על סכמה
מוקטנת של 14 טבלאות עם 4 ספרים. הסכמה האמיתית היא 37 טבלאות, וההפרש
הזה הוא בדיוק המקום שבו מחכות ההפתעות.

מה לבדוק, בסדר הזה:

1. **בנייה** — `SubsetBuilder` על `seforim.db` אמיתי, בחירה קטנה
   (קטגוריה אחת). לוודא: `foreign_key_check` נקי, `quick_check` נקי,
   וכל 37 הטבלאות עברו סיווג בלי `SubsetBuildException`.
2. **זמן וגודל** — כמה נמשכת הבנייה, וכמה באמת חסכה. `SubsetResolver`
   מעריך לפי `book.totalLines`; לוודא שהאומדן קרוב לתוצאה. אם הוא
   סוטה בהרבה, `sizeSafetyFactor` ו-`globalFloorBytes` צריכים כיול.
3. **שקילות על patch אמיתי** — יש קובצי patch על המכונה תחת
   `AppData\Roaming\com.example\launcher_app\otzaria-launcher\`. להריץ
   את שני מסלולי מבחן השקילות עליהם, על 3–4 גרסאות רצופות. **זו
   הבדיקה שקובעת אם המנוע נכון במציאות ולא רק ב-fixtures.**
4. **אוצריא פותחת את התוצאה** — לפתוח את המסד החלקי באוצריא ולבדוק
   קריאה, ניווט, מפרשים וחיפוש.

⚠️ אסור לקרוא תוכן מהמסד בתהליך הזה (סינון נטפרי) — לעבוד מול ספירות,
‏hashes ו-`PRAGMA` בלבד.

---

## 2. ‏[חוסם] דיאלוג שמכבה את עדכוני הספרייה באוצריא

**בלי זה כל השאר חסר משמעות.** אוצריא מאמתת hash תוכן אחרי כל patch,
מסד חלקי נכשל בזה **בהכרח**, וכל כשל מנותב אצלה להורדה מלאה שמוחקת את
הספרייה החלקית. ראו [docs/otzaria-compatibility.md](docs/otzaria-compatibility.md)
§1 עם ההפניות לקוד שלהם.

מה צריך:

* דיאלוג חוסם לפני הבנייה הראשונה, שמסביר ומנחה איך לכבות
* בדיקה חוזרת בכל פתיחה — אם ההגדרה חזרה להיות פעילה, להזהיר שוב
* **לברר**: מה שם מפתח ההגדרה אצל אוצריא, והאם אפשר לקרוא אותו מבחוץ
  כדי לאמת. כתיבה להגדרות שלה מבחוץ שברירית — עדיף לזהות ולהנחות מאשר
  לשנות בכוח.

---

## 3. אפליקציה — ארבעה מסכים

אין `main.dart`. המנוע הוא חבילת לוגיקה בלי קורא.

| מסך | תוכן |
|---|---|
| **מצב הספרייה** | הפרופיל של המחשב הזה: גרסה, כמה ספרים, גודל, קישורים מנותקים, כפתור עדכון |
| **בחירת ספרים** | עץ קטגוריות עם תיבות תלת-מצביות, אומדן גודל **חי**, ופאנל אזהרת הניתוק |
| **התקדמות** | שלבים: הורדה → סינון → החלה → אימות, עם ביטול |
| **הגדרות** | נתיב ה-DB, תיקיית עבודה, מיקום המראה, נתיב האינדקס |

אין מסך בחירת מחשב — `MachineIdentity` מזהה לבד ו-`MachineRegistry`
מוצא את הפרופיל.

**המסך הקשה הוא בחירת הספרים.** עץ של אלפי ספרים שבו כל סימון מפעיל
מחדש את `SubsetResolver.resolve` (שאילתת `link` על המסד המלא). צריך
‏debounce, והרצה ב-`Isolate` — ראו §9 ב-[AGENTS.md](AGENTS.md).

---

## 4. חיווט הזרימה מקצה לקצה

הרכיבים קיימים, אף אחד לא מחבר ביניהם. הזרימה:

```
LibraryUpdateDiscovery  (מהחבילה המעדכנת)  → אילו patches יש
LibraryUpdatePlanner                        → איזה מסלול
PatchDownloader                             → הורדה וחילוץ
SubsetUpdater.applyPatch                    → סינון + החלה   ← שלנו
ProfileStore.save                           → שמירת הגרסה וה-hash
```

ובמסלול שינוי הסכמה: `SubsetRebuilder.rebuild` במקום שני האחרונים.

**מה שחסר בפועל:** שכבת orchestration שמחליטה בין שני המסלולים לפי
`LibraryUpdatePlan`, מריצה ב-`Isolate`, ומדווחת התקדמות ל-UI.

---

## 5. הורדה מלאה בהזרמה

`SubsetRebuilder` מקבל נתיב למסד מלא — מישהו צריך להשיג אותו.

הדרישה: הארכיון הדחוס (~1.5GB) **לא ינחת על הדיסק** — הזרם עובר
מהרשת ישר למחלץ. `PatchDownloader` בחבילה המעדכנת מקבל פונקציית
חילוץ מוזרקת, ושם צריך לחבר zstd בהזרמה.

שיא התפוסה יישאר ~7.4GB + התת-קבוצה. אי אפשר להוריד מזה: SQLite דורש
גישה מקרית, ואי אפשר לשאול זרם שמתפרק. זה המסלול הנדיר (שינוי סכמה
קרה כחמש פעמים בהיסטוריה של הספרייה).

לפני ההורדה — `DiskSpaceProbe` מהחבילה המעדכנת, עם יעד זמני שהמשתמש
יכול לבחור (דיסק חיצוני).

---

## 6. ספרים ממתינים להבאה

`PatchKeepResolution.pendingAcquisition` כבר מזהה ספרים שהמשתמש בחר
ולא קיבל (ספר ותיק שרק נכנס לקטגוריה שנבחרה — ראו §5 ב-AGENTS.md).

מה שחסר: **להציג את זה למשתמש** ולתת כפתור שמריץ `SubsetRebuilder`.
כרגע המידע חוזר ב-`SubsetUpdateResult` ואף אחד לא מסתכל עליו. ספר
שהמשתמש ביקש ולא קיבל חייב להגיע אליו, לא להישאר בלוג.

---

## 7. בדיקות שחסרות

* **`subset_rebuilder_test.dart`** — אין בכלל. לכסות: בנייה מחדש מעל
  ספרייה קיימת, החלפה אטומית, שחזור מ-`.bak` בכשל, `deleteFullDbWhenDone`,
  בחירה ריקה זורקת, מסד בלי `db_schema_version` זורק, וביטול אינדקס
  שנכשל אינו מפיל בנייה מוצלחת.
* **`sqlite_uri_test.dart`** — קידוד נתיבים עם רווחים ועם עברית.
* **בדיקת שקילות על patch אמיתי** — ראו §1.3. זו החשובה מכולן.

---

## 8. החלטות פתוחות

**גיבוי לפני בנייה מחדש.** `SubsetRebuilder` מוחק את המסד המלא בסיום
(`deleteFullDbWhenDone: true`). אם הגיזום יצא שגוי, המשתמש מגלה את זה
אחרי שהמקור נמחק. אולי כדאי להשהות את המחיקה עד אישור מפורש.

**כמה פרופילים מאותו מסד מלא.** `deleteFullDbWhenDone: false` תומך בזה,
אבל אין תזמור שמנצל את זה. מי שמכין כונן לחמישה מחשבים לא צריך להוריד
1.5GB חמש פעמים.

**קטגוריות ריקות אחרי גיזום.** הגיזום שומר צאצאי קטגוריה שנבחרה גם אם
אין בהם ספר (כדי שספר חדש ייקלט). ענפים כאלה יוצגו ריקים באוצריא. זו
החלטה מודעת — האלטרנטיבה שוברת קליטה של ספרים חדשים.

**מה קורה כשהמשתמש מצמצם בחירה.** אם הוא מסיר קטגוריה, המנוע לא מוחק
את הספרים מהמסד הקיים — אין מסלול "גיזום נוסף". צריך להחליט: בנייה
מחדש ממסד מלא, או גיזום in-place. השני מהיר בהרבה אבל דורש קוד חדש.

---

## מה **לא** לעשות

* אל תוסיף `.github/workflows/` — אין CI בריפו הזה במכוון.
* אל תחליש את מבחן השקילות כדי שיעבור.
* אל תכניס ספרים מ-`pendingAcquisition` לספרייה בלי תוכן.
* אל תמחק שורות **בתוך** ספר שנשמר — ראו §7 ב-AGENTS.md.
