# Dart / Flutter
.dart_tool/
.packages
build/
pubspec.lock
*.iml
.idea/
.vscode/

# מסדי נתונים ותוצרי בנייה. אסור שיגיעו לריפו: `seforim.db` הוא 7.4GB
# של תוכן שאינו שלנו, ותת-קבוצה שלו אינה שונה בכך.
*.db
*.db-shm
*.db-wal
*.db.bak
*.db.zst
*.staging.db
*.filtered.db

# תיקיות עבודה זמניות
/tmp/
/work/

# ‏Override מקומי לנתיב החבילה המעדכנת — שונה בין מכונות.
pubspec_overrides.yaml

# תוצר הבנייה של המתקין — נבנה ב-workflow השחרור, לא נשמר בריפו.
installer/output/
