# AGENTS.md

חוזה העבודה על הריפו הזה. כל סעיף כאן היה באג אמיתי או ממצא שאומת —
שינוי הקוד סביבו בלי להבין למה הוא נראה כך יחזיר את הבאג.

**קרא קודם את [README.md](README.md)** — הוא מסביר מה החבילה עושה ולמה
זה בכלל אפשרי. כאן רק מה שאסור לשבור.

---

## שני כללים שחלים על **כל** שינוי

1. **`dart format .` ואז `flutter analyze --no-fatal-infos`.** האנליזה
   חייבת לחזור נקייה לגמרי, כולל `info`.
2. **הערות קצרות, בעברית, ומסבירות *למה*.** שורה או שתיים. הסבר ארוך
   שייך ל-README או לקובץ שב-`docs/`.

**יש CI.** `.github/workflows/test.yml` רץ על כל push ל-`main` ובדרישה
ידנית: format+analyze+test בשתי החבילות (שורש ו-`app/`).
`.github/workflows/release.yml` רץ רק בדרישה ידנית: מעלה patch version
בנעילה הדדית בשני ה-`pubspec.yaml`, בונה, אורז ל-EXE יחיד ומפרסם
Release — ראו §16. שני קבצי ה-workflow משכפלים את `seforim_library_updater`
מ-`Otzaria/Otzaria_Offline_update` לפני הבנייה, כי ה-path dependency
המקומי (`../Otzariya_update`) לא קיים אצל ה-runner.

---

## 1. מבחן השקילות הוא החוזה

`test/equivalence_test.dart` מוכיח:

```
subset_K(apply(patch, full))  ==  apply(filter_K(patch), subset_K(full))
```

זו ההוכחה היחידה שהמסנן נכון. ה-hash החלקי **אינו** מוכיח את זה — הוא
מגבב את מה שיש, לא את מה שהיה צריך להיות.

**כל שינוי ב-`table_scope.dart`, ב-`PatchFilter` או ב-`SubsetBuilder`
חייב להשאיר את הבדיקה ירוקה.** אם היא נופלת, ההנחה היא שהשינוי שגוי —
לא שהבדיקה מחמירה מדי. אל תחליש אותה כדי לעבור.

---

## 2. סיווג הטבלאות

**`kTableScopesInFkOrder` חייב לכסות את `kPatchTablesInFkOrder` בדיוק,
בשני הכיוונים ובאותו סדר.** `table_scope_test.dart` אוכף את זה. טבלה
חדשה בסכמה שלא סווגה תפיל את הבנייה — וזו המטרה: השמטה שקטה שלה הייתה
מייצרת ספרייה חסרה בלי שאף אחד יידע. אותה הגנה חוזרת בזמן ריצה
ב-`SubsetBuilder._assertAllTablesClassified`.

**סדר ה-FK אינו נוח — הוא נכונות.** כל `ParentRef.table` חייב לשבת
**מוקדם יותר** ברשימה מהטבלה שמפנה אליו. הבנייה והסינון מסתמכים על כך
שההורה כבר הועתק כשהצאצא נבדק, ושניהם מפנים לטבלת ההורה **שביעד**
(`main.<parent>`), לא למקור. יש בדיקה שאוכפת את זה.

**אל תשנה `global` ל-`byBook` בלי לבדוק `UNIQUE`.** שמונה טבלאות
גלובליות נושאות `UNIQUE` על עמודת תוכן (`tocText.text`, `author.name`,
`source.name`, `topic.name`, `pub_place.name`, `pub_date.date`,
`generation.name`, `connection_type.name`). שמירתן השלמה היא מה שמשאיר
את התנגשויות ה-`UNIQUE` זהות לאפסטרים; גיזום שלהן מחזיר בדיוק את הבאג
שאפסטרים נכווה בו (issue #19 שם).

**`category` ו-`category_closure` הן היוצא מן הכלל, ולכן מותרות
בגיזום** — ל-`category` אין `UNIQUE` על עמודת תוכן ו-`category_closure`
היא טבלת מפתח טהורה. זו הסיבה היחידה שגיזום העץ בטוח.

---

## 3. ‏SQLite: כיוון החיבור אינו בחירה

**‏SQLite מוריש את דגל הקריאה-בלבד מהחיבור הראשי לכל מסד מחובר.** חיבור
שנפתח `readOnly` אינו יכול לכתוב גם למסד **כתיב** שמחובר אליו — הניסיון
נכשל ב-`SQLITE_READONLY` (code 8). זה הפיל את הגרסה הראשונה של
`SubsetBuilder`.

לכן, בכל מקום: **היעד הוא החיבור הראשי, והקלט מחובר ב-`ATTACH` עם
`mode=ro`** (ראו `sqlite_uri.dart`).

**`uri: true` חובה בפתיחת החיבור הראשי.** בלעדיו SQLite מתייחס ל-URI
כשם קובץ מילולי, לא מוצא, **ויוצר מסד ריק חדש** — כשל שקט שמייצר
תת-קבוצה ריקה בלי הודעה.

**`ANALYZE` בלי שם סכמה מנתח כל מסד מחובר** וכותב `sqlite_stat1` בכל
אחד, כולל המקור לקריאה בלבד. הוא רץ אחרי ה-`DETACH` ומוסמך ל-`main`.

---

## 4. ‏PatchFilter

**`delete_*` מועתקות במלואן, בלי סינון.** `DELETE WHERE pk IN (...)` על
שורה שאינה קיימת הוא no-op. אל "תייעל" את זה — הסינון היה עבודה מיותרת
עם סיכוי לטעות.

**`migrations` לא ריקה → זריקה. אל תהפוך את זה להגדרה.** מיגרציה היא SQL
שרירותי שנכתב בהנחה שהמסד שלם; אחת שממלאת נתונים תיתן על תת-קבוצה תוצאה
אחרת **בשקט**. הכשל הרועש הוא ההגנה, והמסלול הוא `SubsetRebuilder`.

**סינון לפי הורה נבדק מול (מקומי ∪ מה שנשמר מה-patch).** ההורה יכול
להגיע באותה החלה. השמטת האיחוד הזה מפילה כל שורה חדשה שההורה שלה חדש גם
הוא.

**קבוצת הקטגוריות בסינון חייבת להיות זהה לזו שאיתה נבנה המסד.** אחרת
`upsert_category` מחזיר קטגוריה שנגזמה ו-`parentId` שלה מצביע לשורה
חסרה. `categoryPruneWhere` יושב ב-`keep_set.dart` כדי ששני הצדדים
ישתמשו באותו תנאי ולא ייסחפו.

---

## 5. מה שאי אפשר להשיג מ-patch

**ספר שנכנס לבחירה מאוחר אינו מגיע בקובץ עדכון.** patch נושא רק את מה
שהשתנה; ספר שקיים באוצריא כבר כמה גרסאות ורק עכשיו עובר לקטגוריה שנבחרה
מגיע עם שורת `book` בלבד.

`PatchKeepResolution.pendingAcquisition` מחזיק ספרים כאלה, והם **אינם**
נכנסים לספרייה. אל "תתקן" את זה בכך שתכניס אותם — ספר עם שורת `book`
ובלי אף שורת טקסט נראה קיים ונפתח ריק. המסלול הוא `SubsetRebuilder`.

---

## 6. ‏SubsetUpdater

**ההחלה רצה על העתק.** `PatchApplier` של אפסטרים מאמת בתוך ה-transaction
מול `toContentHash` ומגלגל אחורה; על מסד חלקי ה-hash הזה לא יתאים לעולם,
ולכן האימות כבוי — ואיתו נעלם ה-`ROLLBACK`. ה-staging הוא מה שמחזיר את
הבטיחות. **אל תחיל ישירות על המסד של המשתמש.**

**`categoriesPruned` מועבר, לא נגזר.** אי אפשר להסתכל על מסד חלקי ולדעת
אם ענף חסר נגזם או שמעולם לא היה שם. הערך נשמר ב-`SubsetProfile`.

**ה-hash החלקי נושא קידומת `subset:`** כדי שלא ייכנס בטעות לשדה שמצפה
ל-hash של אפסטרים. הוא אינו בר-השוואה אליו.

---

## 7. אסור למחוק שורות **בתוך** ספר שנשמר

אוצריא בונה את טקסט הספר **לפי מקום** (`ORDER BY lineIndex`) אבל פותרת
קישורים ו-TOC **לפי הערך** של `lineIndex`. מחיקת שורות מתוך ספר שנשמר
מכווצת את המספור של הקורא ולא את הערכים המאוחסנים — וכל המפרשים,
ה-TOC ותוצאות החיפוש יינחתו על השורה הלא נכונה.

**המנוע מוחק ספרים שלמים בלבד. זו תכונת נכונות, לא אופטימיזציה.**

---

## 8. שתי דרישות מול אוצריא עצמה

ראו [docs/otzaria-compatibility.md](docs/otzaria-compatibility.md) לפירוט
מלא עם הפניות לקוד של אוצריא.

**המעדכן של אוצריא ימחק את הספרייה החלקית.** מסד חלקי נכשל באימות התוכן
שלה בהכרח, וכל כשל מנותב אצלה להורדה מלאה. חייבים לכבות את עדכון
הספרייה בתוך אוצריא לפני שמניחים מסד חלקי.

**אינדקס החיפוש חייב ביטול אחרי כל גיזום.** הוא יושב מחוץ למסד ואוצריא
**במכוון** אינה מנקה ממנו ספרים רשמיים שנעלמו. `OtzariaIndexInvalidator`
מוחק את התיקייה; הבנייה מחדש זולה דווקא מפני שהספרייה חלקית.

---

## 9. פעולות חוסמות

`SubsetBuilder.build`, `PatchFilter.filter`, `SubsetHasher.compute`,
`SubsetUpdater.applyPatch` ו-`SubsetRebuilder.rebuild` סינכרוניות
וכבדות. עטוף ב-`Isolate.run`, עם closure שקורא לפונקציה **גלובלית**
שמקבלת פרימיטיבים בלבד — closure שנוגע בשדה מופע לוכד את `this` וזורק
`object is unsendable`.

---

## 10. בדיקות

261 בדיקות, כולן על **fixtures סינתטיים**.

**אף בדיקה לא נוגעת במסד אמיתי של אוצריא ואינה מכילה תוכן ספרים.** זו
אינה רק הקפדה על זכויות: בדיקה שתלויה במסד של 7.4GB אינה בדיקה שרצה,
ומה שנבדק כאן הוא לוגיקת הסינון, שאינה תלויה בתוכן. שמור על זה.

`test/fixtures.dart` מחזיק סכמה מוקטנת ששמות הטבלאות והעמודות בה זהים
לאמיתית, כדי ש-`kTableScopesInFkOrder` יחול עליה בלי שינוי.

---

## 11. גיזום במקום — מקור ויעד הם אותו קובץ

`SubsetUpdateFlow.prune` (ב-`app/lib/services/update_flow.dart`) קורא
ל-`SubsetRebuilder.rebuild` עם `fullDbPath` ו-`targetPath` **זהים** —
נתיב ה-`seforim.db` שאוצריא כבר הורידה. אין הורדה נפרדת של מסד מלא
לגיזום הראשוני: המשתמש כבר קיבל אותו דרך אוצריא, ובונים תת-קבוצה חדשה
לצדו (staging), מאמתים, ורק אז מחליפים אטומית.

**`deleteFullDbWhenDone` חייב להיות `false` במסלול הזה.** ברירת המחדל
של `SubsetRebuilder` היא `true` — נכון כשהמקור הוא הורדה זמנית
(`_runFull` בשינוי סכמה, שם המקור באמת נמחק בסוף). כאן המקור **הוא**
היעד; מחיקה "בסיום" הייתה מוחקת את התת-קבוצה שזה עתה נבנתה.

---

## 12. שכבת האפליקציה: עבודה כבדה תמיד ב-`Isolate` דרך `jobs.dart`

`app/lib/jobs/jobs.dart` הוא נקודת המעבר היחידה ל-`Isolate` באפליקציה.
כל פעולה כבדה של המנוע (`SubsetRebuilder.rebuild`,
`SubsetUpdater.applyPatch`, קריאת קטלוג, `SubsetResolver.resolve`)
עטופה שם ב-`JobRequest` שנשלח ל-`Isolate.spawn`, ומדווחת בחזרה
כ-`JobEvent` דרך `SendPort`. ה-UI תמיד קורא דרך `runJob`, לא ישירות
למנוע.

**כל `entry` חייב להיות פונקציה גלובלית שמקבלת `JobRequest<A>` עם `A`
שהוא פרימיטיבים/מודלים פשוטים בלבד.** closure שסוגר על שדה מופע (למשל
`this.subsetPath` בתוך מתודה של `State`) לוכד גם את ה-`State` עצמו —
שמחזיק `BuildContext`, `TextEditingController` וכדומה, שאינם ניתנים
לשליחה — וה-`Isolate.spawn` זורק `Invalid argument(s): object is
unsendable`. ראו גם §9 למעלה, שהוא אותו כלל בצד המנוע.

---

## 13. מנגנון פנימי אסור להגיע ל-UI

גרסאות סכמה, hash-ים, נתיבי staging, שמות שלבי המנוע (`preflight`,
`schema`, `swap`…) ושמות קבצי patch הם פרטי הפעלה של המנוע, לא מידע
למשתמש. `ProgressScreen` מתרגם את שלבי המנוע לארבע תחנות
("מוריד"/"מכין"/"מחיל"/"מסיים"), ו-`HomeScreen` מציג רק ספירת ספרים,
גודל ומצב עדכון — ראו התיעוד בראש שני הקבצים.

**כל מסך או הודעה חדשים חייבים לשמור על זה.** מי שרק רוצה ספרייה קטנה
יותר לא אמור לדעת מה זה `subset:` hash או מה ההבדל בין `delta`
ל-`fullDownload` — אלה נשארים בלוג ובקוד, לא בטקסט שהמשתמש רואה.

---

## 14. הבחירה במסך הפוכה: המשתמש מסמן מה למחוק

**`CategoryTree` ו-`BookSelectionScreen` עובדים על `RemovalSelection` —
מה שסומן *למחיקה*, לא מה שנשאר.** המנוע עצמו לא השתנה ולא ישתנה: הוא
מקבל `SubsetSpec`, כלל של מה *נשאר*, וזה מה שמבחן השקילות מוכיח. התרגום
בין שתי השפות מרוכז ב-`keepSpecFor` (`lib/src/engine/removal_plan.dart`)
— אין להעתיק את הלוגיקה הזו למקום שני.

**ענף שלא נגעו בו נשמר ככלל קטגוריה אחת, לא כרשימת מזהי ספרים.** רשימת
מזהים הייתה תצלום־מצב: ספר שאוצריא מוסיפה מחר לענף שהמשתמש לא סימן לא
היה נכנס אליו לעולם. `keepSpecFor` שומר על כך שענף שלם ומחוץ ולא-נוגע
נכנס כ-`categoryIds`, לא כ-`includeBookIds` — כדי שספר עתידי בו ייכנס
מעצמו, וספר עתידי בענף שנמחק לא ייכנס. **רק ענף מעורב (חלקו סומן)
מתפרק לספרים בודדים.** `test/removal_plan_test.dart` אוכף את זה — שינוי
שהופך ענף שלם-לא-נגוע לרשימת ספרים שגוי גם אם הבדיקות האחרות ירוקות.

---

## 15. זהות חזותית ומינוח — לא כמו אוצריא

**האפליקציה נראית במכוון שונה מאוצריא.** `app/lib/theme.dart` — סגול
אינדיגו עם מבטא טורקיז, גרדיאנט, כרטיסים שטוחים — הרחק מהחום־נייר
המוכר. זו לא העדפת עיצוב: התוכנה **מוחקת ספרים**, ומשתמש שיחשוב שזו
אוצריא יאשים את אוצריא במה שקרה כאן. כל מסך חדש חייב להשתמש בעיצוב
מ-`AppTheme`/`AppColors`, לא בברירת מחדל של Material או בגוונים שמזכירים
את אוצריא.

**טקסט למשתמש אינו נושא מונחים פנימיים.** לא `hash`, לא `patch`, לא
`staging`, לא גרסת סכמה, ולא "subset"/"מסד חלקי" — ראו גם §13. מה
שהמשתמש רואה הוא ספרים, גודל ומצב; איך זה עובד מבפנים נשאר בלוג ובקוד.

---

## 16. ההפצה היא קובץ אחד, והעדכון העצמי תלוי בזה

קובץ ההפצה הוא `stub` (`installer/stub/stub.cpp`) עם `.tar.xz` מודבק
אחריו וחתימה של 32 בתים בסוף. ההרכבה ב-`installer/build.ps1`.

**אין מפרק משלנו, וזו החלטה.** הדחיסה נעשית ב-7-Zip בזמן הבנייה
(‏xz עם מסנן BCJ), והפריסה ב-`tar.exe` שב-System32 — הוא קיים מאז
Windows 10 ונבנה עם liblzma. הגרסה הראשונה דחסה ב-Compression API של
Windows עם מפרק בתוך ה-stub, וזה עלה **2.4MB בהורדה** ועוד כ-150 שורות
קוד. אל תחזיר מפרק פנימי בלי למדוד שוב.

**ה-stub לא מחזיק את המטען בזיכרון.** הוא מעתיק אותו במנות של 1MB
לקובץ זמני ומריץ עליו tar. ‏`ReadAllBytes` על מטען של 9MB שמתפרש ל-30MB
היה מבקש מהמערכת עשרות מגהבייטים בשביל שנייה של עבודה.

**ההיסט של המטען נגזר מגודל הקובץ ואינו נשמר בחתימה.** לכן הבנייה היא
הדבקה פשוטה של שני קבצים, ואין שלב שמתקן מצביעים בתוך ה-PE.

**התיקייה נפרשת ליד ה-EXE, ולא ב-Program Files.** זה מה שמאפשר
ל-`AppUpdater` להחליף את התוכנה בלי UAC. התקנה ל-Program Files הופכת כל
עדכון עצמי לבקשת הרשאות מנהל — ומשתמש שמסרב נשאר תקוע על גרסה ישנה.

**‏`--update <pid> <תיקייה>` ממתין לתהליך לפני שהוא כותב.** כל עוד
התוכנה רצה, ה-DLL-ים שלה נעולים ו-tar נכשל באמצע — עם תיקייה חצי
מוחלפת. ההמתנה היא מה שמונע את זה, והיא הסיבה שה-pid עובר בשורת
הפקודה.

**‏`ShouldDeploy` משווה גרסאות ואינו בודק אי-שוויון.** אחרי עדכון עצמי
התיקייה מחזיקה גרסה חדשה מזו שב-EXE הישן שנשאר לידה; לחיצה עליו חייבת
רק להפעיל. אי-שוויון היה מחזיר את המשתמש אחורה בשקט.

**‏`APP_VERSION` הוא המקור היחיד לגרסה שהתוכנה מכירה על עצמה.** הוא
מוזרק ב-`--dart-define` מאותה שורת `version` שה-workflow מעלה. קבוע
כתוב ביד ב-Dart היה נסחף מה-pubspec בשקט, והתוצאה היא תוכנה שמציעה
עדכון לגרסה שהיא כבר מריצה. `0.0.0` היא בנייה מקומית ואינה בודקת כלום —
מול `releases/latest` היא הייתה נראית ישנה לנצח.

**כשל בבדיקת העדכון מוחזר כ-`null` ואינו נזרק.** אין רשת, מגבלת קצב של
GitHub או תשובה בפורמט לא מוכר — כולם שקטים. מי שפתח את התוכנה כדי
לגזום ספרים לא אמור לראות שגיאה על משהו שלא ביקש.

**שם הקובץ ב-Release אינו חוזה — הסיומת כן.** `AppUpdater` בוחר את
ה-asset הראשון שמסתיים ב-`.exe`, כדי ששינוי של שם הקובץ ב-`build.ps1`
לא ישבור גרסאות שכבר יצאו לשטח.
